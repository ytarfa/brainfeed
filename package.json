{
  "name": "brain-feed",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "sync:types": "pnpm --filter @brain-feed/types run sync"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
