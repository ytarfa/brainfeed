{
  "buildCommand": "pnpm --filter @brain-feed/frontend-error-reporter run build && pnpm --filter @brain-feed/frontend run build",
  "outputDirectory": "apps/frontend/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
