[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/worker-enrichment/Dockerfile"

[deploy]
startCommand = "node packages/worker-enrichment/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
